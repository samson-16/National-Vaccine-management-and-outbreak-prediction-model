# Stale Output Folder

This folder is not used for the defense demo.

Use `model_outputs_xgboost/` as the official frozen output folder for:

- model metrics
- saved XGBoost artifact
- latest predictions
- top-risk woredas
- next-month alerts
- dashboard inputs

The remaining CSV here is an older output kept only because Windows denied moving it during cleanup.
